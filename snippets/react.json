{
  "map": {
    "scope": "typescriptreact,javascriptreact",
    "prefix": "map",
    "body": [
      "${1:array}.map((${2:object}, index) => {",
      "\treturn <>{${2:object}}</>",
      "})"
    ]
  }
}
